import 'package:flutter/material.dart';

class DataScreen extends StatelessWidget {
  const DataScreen({required this.name, required this.dataMap});

  final String name;
  final Map<String, String> dataMap;

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
